[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm build --filter=express-backend"

[deploy]
startCommand = "cd apps/express-backend && pnpm start"

[watch]
paths = ["apps/express-backend/**"] 