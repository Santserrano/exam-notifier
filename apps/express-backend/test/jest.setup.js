// Configuración global para las pruebas
beforeAll(async () => {
    // Aquí puedes agregar cualquier configuración que necesites antes de ejecutar las pruebas
});

afterAll(async () => {
    // Aquí puedes agregar cualquier limpieza que necesites después de ejecutar las pruebas
});

// Configuración global para cada prueba
beforeEach(async () => {
    // Aquí puedes agregar cualquier configuración que necesites antes de cada prueba
});

afterEach(async () => {
    // Aquí puedes agregar cualquier limpieza que necesites después de cada prueba
}); 